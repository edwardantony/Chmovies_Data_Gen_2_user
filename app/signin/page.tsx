import LoginForm from '@/app/components/auth/SigninForm';

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-black">
      <LoginForm />
    </div>
  );
}